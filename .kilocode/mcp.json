{
  "mcpServers": {
    "filesystem": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "--mount",
        "type=bind,src=/home/developer/workspace/,dst=/projects",
        "mcp/filesystem",
        "/projects"
      ]
    }
  }
}